# Server profile: ignore lid switch so closing the laptop screen doesn't
# suspend the machine. This is a headless host — the screen should stay
# closed and the machine should stay running.
[Login]
HandleLidSwitch=ignore
HandleLidSwitchDocked=ignore
HandleLidSwitchExternalPower=ignore
