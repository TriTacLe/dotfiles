# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Arch package cache
*.pkg.tar.zst

# Secrets + per-machine config (never tracked)
.zshenv.secrets
.env.secrets
.env

# Private repo names - not tracked
claude-config/repos.json

# OS artifacts
Thumbs.db
ehthumbs.db
